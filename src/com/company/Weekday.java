package com.company;

public enum Weekday {

    //public static final Weekday MONDAY = new Weekday(); <= basically does the same as this just way less verbose

    MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY;
}
